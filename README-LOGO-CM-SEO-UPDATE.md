# Auto Paint Lab — Logo, Estimate & SEO Update

This update combines:

- Logo displayed at approximately **2× the previous on-site size** in the header, with the logo mark kept visually larger than the business-name text.
- Higher-resolution logo assets with light edge sharpening.
- Estimate form measurement changed from **A4 sheets to cm** (the UI displays cm only).
- Existing pricing structure retained through cm-based size bands so the visual design is unchanged.
- SEO metadata strengthened across the static HTML pages: canonical URL, robots directives, author, Open Graph URL/image, Twitter card metadata.
- React SEO metadata improved with canonical/OG/Twitter URL signals and corrected LocalBusiness URL to `https://autopaintlab.co.nz/`.
- Existing LocalBusiness structured data retained and its logo/image URLs corrected.
- Non-logo static images use lazy loading + async decoding to reduce initial page payload without changing layout.

## Install
Extract this package at the repository root and replace the matching files.
Do not change the existing site design or content outside these updates.
