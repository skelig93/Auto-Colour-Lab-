# Auto Paint Lab — Logo made bigger

## What changed
- **React header** (`SiteHeader.tsx`): logo width increased to 360px → 440px → 500px (was 280/340/360). Header height increased to fit.
- **React footer** (`SiteFooter.tsx`): logo height increased to 11rem / 13rem (was 8rem).
- **Static HTML pages**: `.brand-logo` height increased to ~110–120px (was ~56–76px). Nav bar min-height increased.
- High-resolution logo assets already in `assets/images/auto-paint-lab-logo-clean.webp` (and .png).

## How to upload to GitHub
1. Download `Auto-Paint-Lab-logo-bigger-for-github.zip`
2. Extract it
3. Copy the contents into your repo root (replace existing files with the same names)
4. Commit and push

Key files updated:
- `SiteHeader.tsx`, `SiteFooter.tsx`, `mobile-paint.tsx`
- All static `*.html` pages (index, booking, services pages, etc.)
- Logo assets under `assets/images/`
